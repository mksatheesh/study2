function mult(p1, p2) {
    var r= p1 * p2;   // The function returns the product of p1 and p2
    return (r);
  }
  var res=0;
  res= mult(4,5);
  console.log(`res=${res}`);
  res= mult(6,4);
  console.log(`res=${res}`);
  res= mult(2,10);
  console.log(`res=${res}`);
  res= mult(3,9);
  console.log(`res=${res}`);
